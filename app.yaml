# [START runtime]
runtime: python
env: flex
entrypoint: gunicorn -b :$PORT mysite.wsgi

beta_settings:
    cloud_sql_instances: handz-online:us-central1:handz-postgres-1

runtime_config:
  python_version: 3

# [END runtime]
